<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- WebRTC observes connectivity so it can keep the call's ICE
         connection alive across Wi-Fi / cellular handoffs. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Route in-call audio to a Bluetooth headset. BLUETOOTH_CONNECT is
         the runtime permission required on Android 12+ (API 31); the
         legacy BLUETOOTH / BLUETOOTH_ADMIN cover Android 11 and below. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- FCM push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- WhatsApp-style incoming call: a full-screen-intent notification that
         rings over the lock screen even when the app is closed. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- image_picker uses Android's system photo picker on API 33+, which
         needs NO runtime permission, so READ_MEDIA_IMAGES is deliberately
         NOT declared. This keeps us off Google Play's "photos and videos
         permissions" declaration (the picker is the policy-preferred path
         for one-shot, user-initiated image selection). On Android 12 and
         below image_picker falls back to the legacy gallery, which needs a
         scoped image read — capped at API 32. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- flutter_callkit_incoming declares these Android-14 foreground-service
         permissions, but we only drive CallKit on iOS (IosCallKit is gated to
         iOS; Android uses an FCM + full-screen-intent notification instead). No
         camera/microphone/phoneCall foreground service is ever started on
         Android, so strip the permissions from the merged manifest — otherwise
         they (falsely) trigger Google Play's foreground-service declaration.
         They stay intact in the iOS build, where they're genuinely used. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA"
        tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
        tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"
        tools:node="remove" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Swayco"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Disable Impeller, Flutter's new GPU renderer (default since
             3.27). Impeller has known bugs that surface as a fully
             black screen in Release builds on certain devices (Dart
             runs, widgets build, no crash — just an empty rendering
             surface). Falling back to the legacy Skia renderer fixes
             the issue. Same fix is applied iOS-side via
             FLTEnableImpeller in Info.plist. See
             github.com/flutter/flutter/issues/179592 + 161584. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- FCM: white-silhouette notification icon + accent tint, so
             push notifications show the Swayco logo instead of a white
             square (Android renders the small icon as a monochrome mask). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher: the app opens https links (help, terms, privacy)
             and a mailto: support address. On Android 11+ these intents
             must be declared here or launchUrl / canLaunchUrl fail. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
        <!-- Android native STT (SwayAndroidStt): on Android 11+ the app must
             declare the speech RecognitionService intent to bind to Google's
             on-device recogniser. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
