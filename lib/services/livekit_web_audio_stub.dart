/// Non-web stub. Native platforms (iOS / Android / desktop) control the
/// remote audio volume through `flutter_webrtc`'s `Helper.setVolume`, so
/// there is nothing for this path to do.
void applyLiveKitRemoteVolumeWeb(double volume) {}
