// Dummy translation unit so CocoaPods has something to compile for the
// llm_llamacpp pod. The real native library (llama.framework) is delivered
// by the Dart native-assets build hook, not by this pod.
void _llm_llamacpp_noop(void) {}
