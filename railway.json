{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "Dockerfile",
      ".dockerignore",
      "backend/**",
      "lib/**",
      "web/**",
      "assets/**",
      "pubspec.yaml",
      "pubspec.lock",
      "dart_defines.env",
      "railway.json"
    ]
  },
  "deploy": {
    "healthcheckPath": "/health"
  }
}
